{
  "name": "playbook-de-ia-operacional",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.20.0"
  },
  "scripts": {
    "eval": "mkdir -p .promptfoo-home && find devops -name promptfooconfig.yaml -print | sort | while read -r f; do PROMPTFOO_CONFIG_DIR=$PWD/.promptfoo-home PROMPTFOO_DISABLE_WAL_MODE=true PROMPTFOO_DISABLE_TELEMETRY=true ./node_modules/.bin/promptfoo eval -c \"$f\" || exit 1; done"
  },
  "devDependencies": {
    "npm": "^10.9.8",
    "promptfoo": "^0.119.14"
  }
}
